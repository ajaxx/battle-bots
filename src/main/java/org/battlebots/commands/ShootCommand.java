package org.battlebots.commands;

import org.battlebots.objects.Atom;

/**
 * Command to issue a shot.
 */
public class ShootCommand implements Command {
    /**
     * Apply a command to an atom.
     * @param atom the atom.
     */
    public void apply(Atom atom) {
    }
}
